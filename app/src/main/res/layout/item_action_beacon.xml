<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>

        <variable
            name="viewModel"
            type="com.samebits.beacon.locator.viewModel.ActionBeaconViewModel" />
    </data>

    <FrameLayout
        android:id="@+id/content_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/light_grey" />

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginLeft="12dp"
            android:layout_marginRight="12dp"
            android:layout_gravity="center_vertical"
            android:minHeight="55dp">

            <ImageView
                android:id="@+id/image_enabled_action"
                android:layout_width="wrap_content"
                android:src="@drawable/ic_info_black_24dp"
                android:layout_height="wrap_content"
                android:layout_marginRight="2dp"
                android:layout_alignParentLeft="true"
                android:onClick="@{viewModel.onClickEnable}"
                android:layout_centerVertical="true"/>

            <TextView
                android:id="@+id/text_action_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_toRightOf="@id/image_enabled_action"
                android:padding="16dp"
                android:text="@{viewModel.name}"
                android:textColor="@color/black"
                android:textSize="@dimen/text_title"
                android:textStyle="bold" />


            <ImageView
                android:id="@+id/image_action_delete"
                android:layout_width="wrap_content"
                android:src="@drawable/ic_delete_black_18dp"
                android:layout_height="wrap_content"
                android:layout_marginRight="2dp"
                android:layout_toLeftOf="@+id/text_action_edit"
                android:onClick="@{viewModel.onClickDelete}"
                android:layout_centerVertical="true"/>


            <TextView
                android:id="@+id/text_action_edit"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentRight="true"
                android:clickable="true"
                android:padding="16dp"
                android:text="EDIT"
                android:onClick="@{viewModel.onClickEdit}"
                android:textColor="@color/hn_orange"
                android:textSize="@dimen/text_title" />

        </RelativeLayout>

    </FrameLayout>

</layout>